# Post-merge installed editor verification — 0.7.14

Date: 2026-05-17  
Packages: `@zenithbuild/language@0.7.14`, `@zenithbuild/language-server@0.7.14`  
Note: `main` received an automated `chore(release): v0.7.14 [skip ci]` bump immediately after the `0.7.13` squash merges.

## Step 1 — Local `main`

| Repo | Version | Working tree |
|------|---------|--------------|
| zenith-language | `0.7.14` | clean after `git pull --ff-only` |
| zenith-language-server | `0.7.14` | clean after `git pull --ff-only` |

## Step 2 — Cursor VSIX (0.7.14)

| Check | Result |
|-------|--------|
| Build | `bun run build:marketplace` → `zenith-language-vscode-marketplace.vsix` |
| VSIX version | `0.7.14` |
| Install | `cursor --install-extension …/zenith-language-vscode-marketplace.vsix --force` — success |
| Installed extension id | `zenithbuild.zenith-language` |
| Installed extension version | `0.7.14` (directory `zenithbuild.zenith-language-0.7.14`) |
| Stale duplicate removed | `zenithbuild.zenith-language-0.7.12` directory removed |
| `contributes.languages` | `zenith`, `zen` |
| `contributes.grammars` | `zenith`, `zen` (scope `text.html.zenith`) |
| `activationEvents` | `onLanguage:zenith`, `onLanguage:zen` |

## Step 3 — Manual Cursor checklist

Real file opened for verification: `framework/site/src/pages/index.zen` (local dev project).

| Check | Result |
|-------|--------|
| Language mode | **Zenith** (extension binds `.zen` → `zenith`; user `files.associations` maps `*.zen` → `zenith`) |
| `files.associations` | `"*.zen": "zenith"` (canonical; not stale `"zen"` only) |
| Token inspector screenshots | **Not committed** — requires human UI capture per `docs/manual-cursor-verification.md`. Attach to release notes or PR comment if needed. |
| Grammar scope proxy | `npm test` grammar primitive checks pass; Neovim smoke shows `zenithTag` + `typescriptObjectLiteral` on real file |
| Completion proxy (LSP) | `bun run test:unit` in language-server includes stdio tests: `signal` teaches `.get()`/`.set()`, no `count.value`, no `zenOnMount` |

### Completion contract (stdio, merged language-server `0.7.14`)

Automated LSP stdio test (`script-context completions only teach canonical signal/state/ref API`) confirms:

- `signal` insertText includes `.set(` and `.get()`
- `state` insertText is declarative `state ${name} = …`
- Forbidden labels absent: `zenOnMount`, `value`, `useFetch`, etc.

Bundled `out/server.js` inside the VSIX cannot be executed standalone with `node` (requires extension host module resolution); Cursor runtime uses the embedded bundle.

## Step 4 — Pack / test (merged `main`)

### zenith-language

- `npm test` — **14/14 pass**
- `npm run verify:pack` — **OK** (24 files)
- `npm pack --dry-run` — `zenithbuild-language-0.7.14.tgz`

### zenith-language-server

- `bun run test:unit` — **30/30 pass**
- `npm run verify:pack` — **OK** (6 files)
- `npm pack --dry-run` — `zenithbuild-language-server-0.7.14.tgz`

## Pre-`latest` gate

- **Publish under `next`:** allowed after this document + pack/test pass.
- **Move `latest`:** still blocked until human Cursor token-inspector screenshots are recorded.
